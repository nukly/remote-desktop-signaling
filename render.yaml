# Render.com deployment configuration
# Docs: https://render.com/docs/deploy-node-express-app

services:
  - type: web
    name: adin-rdp-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
